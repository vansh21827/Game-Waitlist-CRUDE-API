# Game-Waitlist-CRUDE-API
Game Waitlist CRUD API is a RESTful backend application built with Node.js and Express.js to manage game waitlists efficiently. The API supports complete CRUD operations using route parameters and follows REST architecture best practices.
